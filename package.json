{
  "name": "yakal-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Tutoring + admissions portal for Yakal Education Services (home + auth + role portals on Postgres/Supabase with RLS).",
  "type": "module",
  "scripts": {
    "start": "npx serve app",
    "test": "node test/e2e.test.mjs",
    "migrate": "node scripts/migrate.mjs",
    "migrate:seed": "node scripts/migrate.mjs --seed"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "pg": "^8.22.0"
  },
  "license": "SEE LICENSE IN LICENSE"
}
